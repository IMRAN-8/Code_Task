name: Update Code_Task Contributions

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Run update
        env:
@@ -22,10 +25,20 @@
          node scripts/update_cf.js

      - name: Commit and push
        env:
          GIT_AUTHOR_NAME: IMRAN-8
          GIT_AUTHOR_EMAIL: 193066785+IMRAN-8@users.noreply.github.com
          GIT_COMMITTER_NAME: IMRAN-8
          GIT_COMMITTER_EMAIL: 193066785+IMRAN-8@users.noreply.github.com
        run: |
          git config --global user.name "IMRAN-8"
          git config --global user.email "193066785+IMRAN-8@users.noreply.github.com"
          git config user.name "IMRAN-8"
          git config user.email "193066785+IMRAN-8@users.noreply.github.com"

          git add data/accepted.json data/summary.json
          git commit -m "Update Codeforces accepted stats" || echo "No changes to commit"
          git push

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Codeforces accepted stats"
            git push
          fi
